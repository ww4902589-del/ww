# 研发资料包 · 2026-09-19 归档

> 归档时间：**2026-09-19 20:06** ／ 最后更新：**2026-09-20 01:36**
> 项目：`<项目目录>`（`APP_VERSION = "2.16"`）
> 归档范围：本轮"日常开发"对话中产生的全部修改记录（成图落盘修复 + 负面提示词注入修复）、
> 待决问题的**修改方案预设**与实施、**升级演练**与**前端改版参考包**、**前端 2+4 合并实施**、
> **参数记忆显式化**、**负面提示词回退放宽（方案A+C）**、**风格注入白名单（§6.2 方案A）**。
> 基线：`run_tests.py` → **383 项，通过 382，失败 0，跳过 1**；DOM 检查 **11/11**。
>
> **更新记录**：① 20:12 用户答复三问（「频繁 风格 保留」），"保留输出目录层级"已实施（§一之二）；
> ② 20:18 用户指派"先做升级演练"，并要求前端参考 → §4 演练完成（8 场景全过）、
> 前端参考包建成（`docs/16` + mockup + 结构基线工具）；③ 21:20 用户指示"继续进行调整，
> 可以参考相似的软件" → **前端 B1+A 实施**：6→5 阶段、导入即预检、真相栏能力事实（§一之四）；
> ④ 22:16 用户指示"进行下一项的修改，将你的修改落实到软件s上" → **§2 方案A「记住这次的参数」
> 实施**（§一之五），并完成 S.exe 重打包（§一之六）；
> ⑤ 00:26 用户拍板「a+c的方案进行」→ **§6.1 负面回退方案A+C 实施**（§一之七）；
> ⑥ 01:07 用户答复「可以」→ **§6.2 风格注入白名单实施**（§一之八）。
> 详见 [15-待决问题修改方案预设](15-待决问题修改方案预设.md) §0.1、§2.1、§3.1、§4、§5、§6.1.1、§6.2.1、§7。

---

## 一、本轮资料清单

| 编号 | 文件 | 落盘时间 | 大小 | 性质 |
|---|---|---|---|---|
| 09 | [成图落盘与预览修复](09-成图落盘与预览修复.md) | 2026-09-19 19:22:20 | 15130 B | 修复记录（**其中"压平文件名"一条已被 15 §1.1 取代**） |
| 10 | [负面编码连接点被占用时的行为分析](10-负面编码连接点被占用时的行为分析.md) | 2026-09-19 19:36:33 | 10086 B | 行为分析（讨论稿） |
| 11 | [负面提示词注入问题查验报告](11-负面提示词注入问题查验报告.md) | 2026-09-19 19:41:30 | 10108 B | 查验报告（含已更正结论） |
| 12 | [负面提示词问题总结与修复计划](12-负面提示词问题总结与修复计划.md) | 2026-09-19 19:46:42 | 8959 B | 修复前定稿清单 |
| 13 | [负面提示词注入修复记录](13-负面提示词注入修复记录.md) | 2026-09-19 20:00:33 | 8200 B | 修复记录（已完成） |
| 14 | 本文（资料包归档索引） | 2026-09-19 20:06 | — | 索引 + 待决问题预设 |
| 15 | [待决问题修改方案预设](15-待决问题修改方案预设.md) | 2026-09-19 20:07 | — | 六问题方案 + 用户答复与实施 |
| 16 | [前端改版参考](16-前端改版参考.md) | 2026-09-19 20:38 | — | 三层防线 / id 速查 / 合并 mockup / 不许坏清单 |

> 编号 01–08 为前几轮既有资料（2026-09-15 落盘），本轮未改动，清单见文末「三、既有资料」。

---

## 一之二、20:12 之后追加的代码改动

用户答复「保留」输出目录层级，因此 `docs/09` 里"把文件名压平成裸名"那条**被取代**：

| 项 | 变化 |
|---|---|
| `src/comfybatch_v2_core.py` | `copy_output_image()` 新增 `keep_structure=True` 与两个私有辅助 `_output_relative_parts()` / `_deduplicate_target()`；保留批次子目录、丢掉 `ComfyBatch-V2` 那一层 |
| `tests/test_output_paths.py` | 18 → 22 项（+4 新增，2 项改写断言） |
| 测试基线 | 360 → **364** |

## 一之三、20:18 指派的升级演练与前端参考

用户指派"先做前者（升级演练）"，并要求前端改动需要参考：

| 项 | 内容 |
|---|---|
| `tests/test_upgrade_drill.py` | **新增 8 项**（真实文件 + 真实重启的升级演练：删除不复活 / 旧副本高 revision 仍输给墓碑 / 安装目录只读 / 主文件损坏时恢复副本接管 / 新 id 重建不受墓碑误伤 / 全删后更新保持为空 等） |
| `run/snapshot_frontend.py` | **新增工具**：前端结构基线（六阶段 ids/按钮/标签、设计令牌、断点、布局规则），细粒度 diff |
| `run/frontend_baseline.json` | 当前结构的基线快照（已建立） |
| `run/mockup-stage24.html` | 阶段 2+4 合并方案的静态 mockup（用当前设计令牌，不接接口） |
| `docs/16-前端改版参考.md` | 改版三层防线、id 速查、**阶段3与4边界陷阱**、合并分步走法、不许坏五条 |
| 测试基线 | 364 → **372** |

**演练结论**：删除的预设不会在更新后复活——旧副本即使自称更高 revision 也赢不过墓碑。
设计成立，无需改动。

详见 [15-待决问题修改方案预设](15-待决问题修改方案预设.md) §4、§5 与
[16-前端改版参考](16-前端改版参考.md)。

## 一之四、21:20 指派的前端合并实施（B1 + A）

用户指示"继续进行调整，可以参考相似的软件"。参考 **SwarmUI Simple Tab**
（选流 → 参数同屏）与 **Fooocus**（主屏常驻、高级就近展开）：

| 项 | 内容 |
|---|---|
| B1 真相栏能力事实 | 新增「采样」「输入输出」两行，任何阶段可见；与面板 facts 共用 `effectiveFacts()` |
| A 阶段 2+4 合并 | 「工作流与参数」单面板（`data-step=2`，`id=paramWorkbench` 归位其上）：左工作流+facts / 右参数工作台；6→5 阶段重排（检查与运行→4、成图与确认→5） |
| 导入即预检（新增） | 两条导入路径成功后自动 POST `/api/preflight`（与「实际检查」同一后端，无新口径），横幅显示在合并面板顶部，"能不能跑"提前到导入后几秒 |
| 改动文件 | `src/index.html`、`src/app.js`（`LAST_STEP=5`、惰性加载改阶段 2、跳转目标、`precheckAfterImport()`、`renderRail()`）、`src/app.css`（`.merge-grid`、`.precheck`、800px 折叠）、`tests/test_page_assets.py`（+2）、`run/frontend_baseline.json`（按五阶段重建） |
| 测试基线 | 372 → **374**（+2）；DOM 11/11；结构基线 diff 127 条逐条确认后 `--update` 归零 |

B2"观察手感"按用户指示跳过，B1 与 A 一轮完成。实施记录详见 `docs/15` §3.1。

## 一之五、22:16 指派的参数记忆显式化（§2 方案A）

用户指示"进行下一项的修改"。按 `docs/15` §7 顺序，下一项为 **§2 方案A「记住这次的参数」**：

| 项 | 内容 |
|---|---|
| UI 开关 | 参数工作台作用范围行新增复选框「记住这次的参数（保存为该工作流默认值）」，**仅"整个批次"范围显示**；语义＝勾上后应用等于现有 `remember()`，零新增猜测 |
| 后端 | `/api/params/apply` 新增 batch+remember 分支，与 workflow 范围完全同构（合法值合并保存、清空的键从默认值移除，越界值照旧不落盘） |
| 改动文件 | `src/index.html`、`src/app.js`、`src/app.css`（`.wb-remember` + `[hidden]` 覆盖）、`src/comfybatch_v2_app.py`、`tests/test_app_routes.py`（+1）、`tests/test_page_assets.py`（+1） |
| 测试基线 | 374 → **376**（+2）；DOM 11/11；结构基线 diff 2 条（新 id）确认后归零 |

hidden 切换守卫测试反向抓出 `.wb-remember` 初版缺 `[hidden]` 覆盖规则——守卫生效，已修。
实施记录详见 `docs/15` §2.1。

## 一之六、22:16 的 S.exe 重打包与副本归一

查明"软件 S"的真身：`src/S.spec`（PyInstaller 单文件，打包 `comfybatch_v2_app.py` +
`index.html`/`app.css`/`app.js`/`default_presets.json`）→ `build/dist/S.exe`。
旧 exe（09-15 16:53）早于本轮全部前端改动，已按 `S.spec` 重新打包（16.1 MB，22:19），
冒烟测试确认打包版页面带「记住这次的参数」开关。
（沙箱注记：打包版在沙箱内以默认端口启动会秒退 exit 0——沙箱对 8790/8791 端口做了
TCP 虚拟化，`is_comfybatch_server` 探测被误答走了"复用已有实例"路径；换 9977 端口即正常服务。）

**副本归一**（用户确认"覆盖桌面并清掉资料包副本"）：

| 副本 | 处置 |
|---|---|
| `<桌面目录>\S.exe`（168.9 MB，09-14 胖版） | 先备份至 `run\backups\S.exe.2026-09-14-fat.bak`，再用新 exe 覆盖 |
| `S-研发资料包-2026-09-14-v1\01-当前程序\S.exe`（168.9 MB） | 经回收站 API 移除（同字节内容已在 fat.bak 中） |
| `build\dist\S.exe` | 新版本体（与桌面同一构建） |
| `run\backups\S.exe.2026-09-15.bak` | 09-15 版回退件，保留 |

`src/ComfyBatch-V2.spec` 为前身旧 spec，未再使用。README 的回退说明已同步改写。

## 一之七、00:26 拍板的负面回退放宽（§6.1 方案A+C）

用户拍板「a+c的方案进行」（B 方案明确不做）。核心：把"没有编码器就硬拒"改为
**三级决策梯**——就地借 → 全图就近回退 → 按作者原样挂空负面并如实相告：

| 项 | 内容 |
|---|---|
| 方案A CLIP 全图回退 | 新增 `_fallback_clip_sources()`：全图扫描 `CLIPTextEncode` 的 clip 输入 / `CLIPLoader` 输出0 / `CheckpointLoaderSimple` 输出1，按节点 id 数字序取第一个；`_apply_prompt` 第一跳借不到时自动回退，审计记 `negative_clip_fallback` |
| 方案C 空负面如实相告 | 全图无 CLIP 来源时不再抛"无法从正面编码器取得 CLIP"，保留作者原接线（`ConditioningZeroOut` 语义），审计记 `negative_skipped`，warning 明说「负面词不会生效」 |
| capabilities 同步 | `negative_prompt` 载荷新增 `mode`（未填写 / 空负面（填了但不生效） / 自动写入或补建 CLIPTextEncode）、`empty_negative`、`fallback_expected`；"将改为直接负面引导" warning 只在绕行确实可行时发 |
| 前端真相栏 | 追加 ` · CLIP 来源为回退所得` 与 ` · 负面词不生效` 两个标注（app.js ~672） |
| 改动文件 | `src/comfybatch_v2_core.py`（`__init__` 两个记账 dict、build() 重置、capabilities 重构、payload、`_fallback_clip_sources`、`_apply_prompt` 三级梯、audit 输出）、`src/app.js`、`tests/test_comfybatch_v2_core.py`（−1 硬拒测试，+3 新测试） |
| 测试基线 | 376 → **378**；DOM 11/11；结构基线一致 |

实施记录详见 `docs/15` §6.1.1。

**本轮重打包**（00:32）：`S.spec` → 16,835,321 B（≈16.1 MB），`build\dist\S.exe` 与
`<桌面目录>\S.exe` 已同步为新构建；旧构建备份至 `run\backups\S.exe.2026-09-19.bak`。
打包产物校验改用 `CArchiveReader.extract("app.js")` 抽取比对（84,105 B，§6.1 标记全命中）。
沙箱注记（两条新坑）：① PyInstaller 覆盖旧 exe 时其 `os.remove` 被沙箱 shim 拦截转回收站而失败，
ctypes 回收站亦被拦（rc=120）——**绕法：先备份旧 exe，打包到 `--distpath build\dist2` 新目录，
再 `Copy-Item -Force` 覆盖回 dist**；② 覆盖静默失败的根因是**上轮冒烟残留的 S.exe 进程
（PID 31272）锁着 dist\S.exe**，`Stop-Process` 后覆盖即成功；③ 本轮沙箱内打包版
**冷启动秒退**（新旧行为一致，对照实验确认），运行时冒烟无法进行，以"378 项全过 +
产物内容抽取校验"替代。

## 一之八、01:07 拍板的风格注入白名单（§6.2 方案A）

用户答复「可以」后实施。**白名单**（`STYLE_INJECTION_WHITELIST`，首批唯一条目
`easy stylesSelector`）每条目自带 docs/15 预设的三件事：① 类型；② 锚点（采样器
`positive` 直连 `CLIPTextEncode`）；③ 消费口（组合器链输出汇回该编码器 `text` 口）。
`easy stylesSelector` 是字符串级组合器，锚点契约照实收窄到 text 口，不做 conditioning 级注入：

| 项 | 内容 |
|---|---|
| 回退档 | 原生模式 + 锚点不满足：由 build 抛错改为**回退提示词编译**（`apply_style_templates` 与 compile 共用合并实现），warning 明说；同时绕开作者自带组合器避免双重风格 |
| 记账 | audit `style_injection`：`mode` / `hosts` / `unanchored` / `injected_nodes`（逐个标 `SOURCE_INJECTED`，取代 `_record_injected_styles` 启发式）/ `displaced`（被挤掉的 text 来源） |
| capabilities | `style.native_anchor` / `native_fallback` 新键；预告与 build 共用 `_style_anchor_hosts`（API/UI 图通用）；锚点+模板双缺→预检即拒 |
| 真相栏 | 回退时风格行追加「 · 原生锚点不可用，已改编译」 |
| 改动文件 | `src/comfybatch_v2_core.py`（白名单、`_style_anchor_hosts`、`_positive_target_type`、`_apply_native_styles` 重构、两处风格分支、capabilities、audit、`PromptCompiler.apply_style_templates`）、`src/app.js`、`tests/test_comfybatch_v2_core.py`（新增 `NativeStyleInjectionTests` 5 项）、`tests/test_comfybatch_v2_app.py`（1 处过期文案更新） |
| 测试基线 | 378 → **383**（+5）；DOM 11/11；结构基线一致 |

实施记录详见 `docs/15` §6.2.1。**至此 docs/15 §7 执行顺序表全部收口。**

---

## 二、09–13 一句话提要

| 编号 | 一句话 |
|---|---|
| 09 | 修掉"成图存不下来"（ComfyUI 两种上报形状导致路径被拼两遍，且 `OSError` 逃逸止损）与"存下来看不动"（每次重渲染重拉全尺寸原图），预览数据量降 99.4%。 |
| 10 | 回答"负面连接点被占用还能不能继续"——能，已实测跑通；顺带摊开该极清流的真实接线（作者用 `ConditioningZeroOut(正面)` 当负面，等于"不做负面引导"）。 |
| 11 | 查验四项问题，全部复现；其中"问题 4"（作者原词被静默丢弃）当时判为严重。 |
| 12 | **更正 `docs/11` 的问题 4**——真实导出图自带 `widget` 声明，故降级为窄口"问题 5"，并给出 P0–P3 修复优先级。 |
| 13 | 落地 P0-2 语义变更 warning、P1 负面节点复用、P2 内置控件顺序兜底表、P3 孤立节点标记、前端真相栏展示。 |

### 本轮改动落到哪些文件

| 文件 | 改动 |
|---|---|
| `src/comfybatch_v2_core.py` | `NEGATIVE_SURROGATE_TYPES`、`_negative_anchor_types()`、`last_orphaned_conditioning`、`capabilities()` 新增 warning 与 `negative_prompt.replaces`、`_apply_prompt` 改为实例方法并加复用表、`audit()` 输出 `orphaned_conditioning` |
| `src/comfybatch_nodeschema.py` | 新增 `BUILTIN_WIDGET_ORDER`（15 类核心节点的**声明顺序**），`widget_order()` 末尾兜底 |
| `src/app.js` | 真相栏「负面提示词」一行追加 `· 将替换 X` |
| `tests/test_comfybatch_v2_core.py` | 新增 6 项（语义变更 warning / 真编码器不误报 / UI 图 link 解析 / 单编码器复用 / 孤立节点保留 / 无提示场景） |
| `tests/test_widget_binding.py` | 新增 2 项（兜底表生效 / schema 优先） |
| `src/comfybatch_v2_core.py` | **（20:20 追加）** `copy_output_image()` 加 `keep_structure` + `_output_relative_parts()` / `_deduplicate_target()` |
| `tests/test_output_paths.py` | **（20:20 追加）** 18 → 22 项 |

### 测试基线变化

| 时点 | 结果 |
|---|---|
| 本轮起点（`docs/09` 之后） | 352 项，通过 351，失败 0，跳过 1 |
| 负面提示词修复完成（20:00） | 360 项，通过 359，失败 0，跳过 1（+8） |
| 输出目录层级实施后（20:20） | 364 项，通过 363，失败 0，跳过 1（+4） |
| **升级演练加入后（20:38）** | **372 项，通过 371，失败 0，跳过 1**（+8） |
| **参数记忆显式化后（22:16）** | **376 项，通过 375，失败 0，跳过 1**（+2） |
| **负面回退方案A+C 后（00:26）** | **378 项，通过 377，失败 0，跳过 1**（−1+3） |
| **风格注入白名单后（01:36）** | **383 项，通过 382，失败 0，跳过 1**（+5） |
| DOM 无头检查 | 11/11 |

---

## 三、既有资料（2026-09-15 落盘，未改动）

| 编号 | 文件 | 大小 |
|---|---|---|
| 01 | 根因分析.md | 11333 B |
| 02 | 架构说明.md | 16063 B |
| 03 | 验收记录.md | 20474 B |
| 04 | 资源替换与种子.md | 9235 B |
| 05 | 参数工作台.md | 8666 B |
| 06 | 单实例与多页面同步.md | 6190 B |
| 07 | 预设持久化与数据安全.md | 8348 B |
| 08 | 前端重新规划.md | 10078 B |

---

## 四、待决问题与修改方案预设

以下六个问题在对话里提出过、已做过事实核查。用户已于 **20:12** 答复三个提问
（「频繁 风格 保留」），其中 §1 已实施。
逐项全文见 [15-待决问题修改方案预设](15-待决问题修改方案预设.md)。

| # | 问题 | 现状事实 | 结论 / 方案 | 状态 |
|---|---|---|---|---|
| 1 | 自由选择存储位置 | **已可自由选择** | 用户选**保留**批次子目录 → 已实施（保留 `<run>/`，丢掉 `ComfyBatch-V2` 那层） | ✅ **已完成** |
| 2 | 工作流参数自动映射 | **不存在自动映射**；只有按名绑定 + 已存默认值 | 保守方案：只做"上次值继承" | 待做 |
| 3 | 确认流程顺序优化 | 现为六阶段串行 | 用户**频繁换工作流** → 合并「工作流+参数」值得做；mockup 与约束见 `docs/16` | 待做（参考已备好） |
| 4 | 删除是否会在更新后复活 | **代码已防**（墓碑并集 + 单一权威文件） | 升级演练 8 场景全过，**设计成立，无需改动** | ✅ **已验证** |
| 5 | 前端设计可否改变 | **可以**；只有资源级测试，无视觉回归 | 结构基线工具 + mockup + 参考包已建成（`docs/16`） | 🔶 **地基已建成** |
| 6 | 工作流适配放宽（负面回退、节点注入） | 部分是"故意的保守"；部分是能力空缺 | 负面回退用**显式开关**；节点注入白名单**限定风格类** | 待做 |

---

## 五、归档说明

- 本资料包沿用既有 `docs/NN-名称.md` 约定，编号连续，不覆盖历史文件。
- `docs/11` 中含一处**已被 `docs/12` 更正**的过度泛化结论，`docs/09` 中含一处
  **已被 `docs/15` §1.1 取代**的"压平文件名"做法——**均按原样保留**以留存研发过程，
  阅读时以更正/后续文档为准。这是有意的：资料包里记录"当时怎么判、后来怎么改判"，
  比只留最终结论更有研发价值。
- 每处被取代的结论都在就地加了指向后续文档的标注，不会留成孤立陷阱。
- `run/` 目录下的 `_verify_*.py` / `_*.txt` 为可复现调试产物，随包保留；
  其中若干 `_*.py` 因沙箱 trash 机制报 `SAFE_DELETE_FAIL_CLOSED` 删不掉，无害。
